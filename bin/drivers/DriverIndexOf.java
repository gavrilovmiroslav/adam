

public class DriverIndexOf {
	public static void main(String[] args) {
		char[] source;
		int sourceOffset;
		int sourceCount;
		char[] target;
		int targetOffset;
		int targetCount;
		int fromIndex;
		String test;
		test.indexOf(source, sourceOffset, sourceCount, target, targetOffset, targetCount, fromIndex);
	}
}
