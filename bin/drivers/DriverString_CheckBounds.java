
import java.io.*;
import java.util.*;

public class DriverString_CheckBounds {
	public static void main(String[] args) {
		byte[] bytes;
		int offset;
		int length;
    		String.checkBounds(bytes, offset, length);
	}
}
