
import java.io.*;
import java.util.*;

public class DriverString_IndexOf {
	public static void main(String[] args) {
		char[] source;
		int sourceOffset;
		int sourceCount;
		char[] target;
		int targetOffset;
		int targetCount;
		int fromIndex;
    		String.indexOf(source, sourceOffset, sourceCount, target, targetOffset, targetCount, fromIndex);
	}
}
