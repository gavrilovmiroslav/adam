

public class DriverGetToomSlice {
	public static void main(String[] args) {
		int lowerSize;
		int upperSize;
		int slice;
		int fullsize;
		BigInteger test;
		test.getToomSlice(lowerSize, upperSize, slice, fullsize);
	}
}
